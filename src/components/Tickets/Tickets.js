import * as React from "react";

const Tickets = () => {
  const addTicket = () => {};

  return (
    <>
      <h1>Tickets Page</h1>
      <br />
      <button onClick={addTicket}>Add Ticket</button>
    </>
  );
};

export default Tickets;
